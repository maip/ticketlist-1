<h1>TicketList</h1>

<p>Your TicketList is waiting for you. Click on the thumbnail of an event to find out more.</p><table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Event type</th>
      <th>Date</th>
      <th>Time</th>
      <th>Venue</th>
      <th>Available tickets</th>
      <th>Total tickets</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.title %></td>
        <td><%= event.event_type %></td>
        <td><%= event.date %></td>
        <td><%= event.time %></td>
        <td><%= event.venue %></td>
        <td><%= event.available_tickets %></td>
        <td><%= event.total_tickets %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %><br><br><p>Want to see your booked tickets? <%= link_to 'View now.', tickets_path %></p>
